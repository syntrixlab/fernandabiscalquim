export type { ButtonBlockData } from '@/types';
import type { ButtonBlockData } from '@/types';

export const buttonDefault: ButtonBlockData = {
  label: 'Saiba mais',
  href: '',
  variant: 'primary',
  newTab: false
};
